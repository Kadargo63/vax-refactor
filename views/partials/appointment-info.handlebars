<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/appointment/{{id}}">{{vax_service}}</a></h4>
        <p>Scheduled by {{user.username}} on {{format_date created_at}}</p>
    </div>
        <p class="card-body">{{content}}</p>
</div>